enum elementConditions { // TODO: is it actually needed???
  visible = 'isDisplayed',
  viewport = 'isDisplayedInViewport',
  enabled = 'isEnabled',
  existing = 'isExisting',
  focused = 'isFocused',
  clickable = 'isClickable',
}

export default elementConditions;
